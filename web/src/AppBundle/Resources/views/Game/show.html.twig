    {% extends 'AppBundle::baseAdmin.html.twig' %}

    {% block body -%}
    <h1>Game</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                    <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Active</th>
                    <td>{{ entity.active|fontAwesome() }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="list-inline">
    <li>
        <a class="btn btn-primary" href="{{ path('admin_games.index') }}">
            <i class="fa fa-arrow-left"></i> Back to the list
        </a>
    </li>
            <li>
            <a class="btn btn-primary"
               href="{{ path('admin_games.edit', { 'id': entity.id }) }}">
                <i class="fa fa-edit"></i> Edit
            </a>
        </li>
                <li>{{ form(delete_form) }}</li>
    </ul>
    {% endblock %}
